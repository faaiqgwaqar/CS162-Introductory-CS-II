Discovered issue in read file for data.tsv, but never got to correcting the segfault. Tries to access unavailable data through the wile loop, fixed by changing iteration, another issue arised from the data type accessing the id, which was initially implemented as an int, but had chars as well so reimplemented as a string. Finally, the comparison neead a less than rather than a greater than
